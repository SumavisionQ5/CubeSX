# WiiSX / CubeSX

## 许可协议

该软件基于 **GNU 通用公共许可证 v2** 进行许可，详细内容请访问：
[http://www.gnu.org/licenses/gpl-2.0.txt](http://www.gnu.org/licenses/gpl-2.0.txt)

这要求任何发布的修改都需采用相同许可，并且提供源代码。

WiiSX/CubeSX 及其相关标志是 **Team Wii64** 的商标，未经授权不得在非官方版本中使用。

---

## 快速使用

* 支持的 ISO 格式：`.bin/.cue`（确保 .cue 文件包含相对路径！）、`.img` 或 `.iso`。
* 安装方法：将 `wiiSX-<版本号>.zip` 的内容解压到 **SD 卡根目录**。
* SD 卡检测顺序：

  * **GameCube**：GCLoader → SD2SP2 → 插槽 A → 插槽 B
  * **Wii**：前置 SD → 插槽 A → 插槽 B
* **SD/USB**：将 ISO 文件放入 `/wiisx/isos` 目录，所有存档会自动保存到 `/wiisx/saves`。
* **DVD**：ISO 可以放在光盘任意位置，需为 ISO9660 格式。

  * GameCube：当前因 libOGC 问题无法使用（XenoGC 可能仍然可行）。
  * Wii：需要能读取 DVD-R 的光驱。
* **BIOS**：将 `SCPH1001.BIN` 放入 `/wiisx/bios` 目录（SD/USB）。
* 从 HBC 或其他加载器启动可执行文件 → 选择 **Load ISO** → 选择来源并加载 ISO。

  > 上级目录选择 `..`，B 键退出文件浏览器。
* 选择 **Play Game** 开始游戏。

  * 可随时按下组合键退出游戏：

    * GC 手柄：START + X
    * 经典控制器：HOME
    * Wiimote：- + +
    * Wiimote + Nunchuck：1 + 2
    * 或按 Reset 键
  * 注意：退出是保存游戏的唯一方式，不会自动保存。

---

## 控制

* 按钮可完全自定义映射。
* 配置界面会显示所有 PSX 按钮，逐一切换来源。
* 每种手柄有 **4 个配置槽**：

  * 点击“Load”加载之前保存的配置。
  * 设置完成后点击“Save”保存到该槽。
  * 记得在设置 → 输入标签中保存整体配置。
* 点击“Next Pad”可切换已分配的 PSX 控制器。
* 模拟摇杆 Y 轴可反转（默认 Normal Y）。
* “Menu Combo” 可配置返回菜单的组合键。

---

## 设置

### 常规

* **Native Saves Device**：选择存档保存位置。
* **Save States Device**：选择即时存档保存位置。
* **Select CPU Core**：选择解释器（兼容性更好）或动态重编译（速度更快）。
* **Save settings.cfg**：将设置保存到 SD/USB，下次自动加载。

### 视频

* **Show FPS**：显示帧率。
* **Screen Mode**：选择宽高比（强制 16:9 会在两侧加黑边）。

### 输入

* **Configure Input**：选择手柄。
* **Configure Buttons**：进入手柄配置界面。
* **Save Button Configs**：保存配置到 SD/USB。
* **Auto Load Slot**：选择自动加载的槽位。

### 音频

* **Disable Audio**：静音。

### 存档

* **Auto Save Native Saves**：启用后会在加载 ISO 时自动加载存档，返回菜单或关机时保存。
* **Copy Saves**：未实现。
* **Delete Saves**：未实现。

---

## 兼容性

遇到问题请千万不要反馈至：
[https://github.com/emukidid/pcsxgc/issues](https://github.com/emukidid/pcsxgc/issues)
这个版本默认开启高速读盘，兼容性不如原版，对于生化危机、少年街霸3等热门游戏有兼容性问题，但对KOF97等游戏，有明显加快读盘的效果。
所以有兼容性问题也不要反馈到原版pcsxgc页面。

---

## 致谢

* **一般编码**：emu_kidid
* **图形 & 菜单编码**：sepp256
* **音频 & 核心编码**：tehpola
* **美术**：drmr
* **pcsx 团队**：[http://www.pcsx.net/](http://www.pcsx.net/)
* **notaz** (pcsx-ReARMed)
* **pcercuei** (lightrec) [https://github.com/pcercuei/lightrec/](https://github.com/pcercuei/lightrec/)
* 基于 **Gil Pederson** 的 pcsx 1.5-test3 Mac 版 [http://pcsx.gpost.dk/](http://pcsx.gpost.dk/)
* **P.E.Op.S. PSX GPU & SPU** [http://sourceforge.net/projects/peops/](http://sourceforge.net/projects/peops/)
* **SSSPSX**
* 使用 **devKitPro/libOGC** 编译 [http://sourceforge.net/projects/devkitpro](http://sourceforge.net/projects/devkitpro)
* 加入我们的社区：IRC EFNet #gc-forever / [www.gc-forever.com](http://www.gc-forever.com) / [GitHub](https://github.com/emukidid/pcsxgc/)

---

## 更新日志

> 请在 GitHub Releases 查看最新更新，本节不再维护。

### Beta 4.0

* 正式集成 libpsxcore。
* 切换到上游 unai GPU 插件，性能显著提升。
* 三重缓冲。
* 构建系统改为 cmake。

### Beta 3.0

* 新 dynarec (lightrec)，提升速度和兼容性。
* 同步 pcsx-ReARMed 上游，彻底解决 Wii 上 XA/CDDA 卡顿。
* dfsound 音频插件。
* 软图形插件优化（感谢 Extrems）。
* GameCube 支持 SD2SP2。
* 修复 UI bug 与内存泄漏。

### Beta 2.1

* 使用 devkitPPC r21 / libOGC SVN 编译。
* 使用新 libDI / DVDx V2。
* **保存改进**：修复未写入存档问题，支持音频状态保存/加载。
* **手柄改进**：修复 Y 轴反转、震动、无效端口输入，支持 CC Home 键，添加禁用震动。
* **网络改进**：修复 SMB 匿名登录，线程化网络初始化并提示错误信息。

### Beta 2

* 使用 devkitPPC r21 / libOGC SVN 编译。
* 使用新 libDI / DVDx V2。
* DVD 读取改为直接 PowerPC 访问。
* 支持 Samba 加载。
* 支持执行 BIOS。
* **手柄改进**：Wiimote 输入支持震动，支持经典控制器 Pro & 第三方，按钮可重映射并保存/加载。
* **新菜单系统**：支持 CC、Wiimote、Nunchuck，支持设置保存和自动存档。
* **PEOPS GPU**：强制 16:9 加黑边，修复 FMV 色彩映射。
* **FranSPU**：全帧平滑立体声音频。
* **SSSPSX 输入插件**：支持 DualShock、震动、模拟/数字切换。
* **CDRMooby**：改进兼容性（CDDA 尚未实现）。

### Beta 1

* 音频（有轻微卡顿）。
* DVD 加载。
* 软件图形 + GX 缩放。
* 保存到 SD 卡。
* 文本 UI。
* **已知问题**：

  * 未实现 CDDA 音频流。
  * XA 音频偶尔失败。
  * 《最终幻想 VII》在 dynarec 下会崩溃。
  * FPS 限制偶尔失效。
