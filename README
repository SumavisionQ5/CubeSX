
遇到问题请千万不要反馈至：
[https://github.com/emukidid/pcsxgc/issues](https://github.com/emukidid/pcsxgc/issues)
这个版本默认开启高速读盘，兼容性不如原版，对于生化危机、少年街霸3等热门游戏有兼容性问题，但对KOF97等游戏，有明显加快读盘的效果。
所以有兼容性问题也不要反馈到原版pcsxgc页面。

---
README : WiiSX / CubeSX

许可协议：
该软件基于 GNU 通用公共许可证 v2 进行许可，详细内容请访问：http://www.gnu.org/licenses/gpl-2.0.txt

这要求任何发布的修改都需采用相同许可，并且提供源代码。

WiiSX/CubeSX 及其相关标志是 Team Wii64 的商标，未经授权不得在非官方版本中使用。

快速使用：

支持的 ISO 格式：.bin/.cue（确保 .cue 文件包含相对路径！）、.img 或 .iso 格式。

安装方法：将 wiiSX-<版本号>.zip 的内容解压到 SD 卡的根目录。

SD 卡检测顺序如下：

GameCube：GCLoader、SD2SP2、A 插槽、B 插槽。

Wii：前置 SD、A 插槽、B 插槽。

对于 SD/USB：将 ISO 文件（.bin/.cue 或其他格式）放入名为 /wiisx/isos 的目录中，所有保存类型将自动放入 /wiisx/saves。

对于 DVD：ISO 文件可以放在光盘的任何位置，必须是 ISO9660 格式。

GameCube：由于 libOGC 在备份的兼容性问题，当前无法使用，XenoGC 可能仍然有效。

Wii：需要支持读取 DVD-R 的光驱。

对于实际的 BIOS：将 SCPH1001.BIN 放入名为 /wiisx/bios 的 SD/USB 目录中。

从 HBC 或所选的加载器中加载可执行文件。加载后，选择“加载 ISO”并选择要加载的 ISO。
（注意：要上级目录，请选择“..”，按 B 键退出文件浏览器）

选择“玩游戏”开始游戏。
游戏可以随时通过按下 GC 手柄的可配置按键组合（START 和 X）、经典控制器（HOME）、Wiimote（- 和 +）、Wiimote + Nunchuck（1 和 2），或者复位按钮退出。
（注意：必须这样做才能保存游戏，退出时不会自动保存。）

控制：

控制完全可配置，可以将任意按钮映射到手柄。

控制配置屏幕会显示每个 PSX 按钮，并允许切换源。

每种类型的控制器有 4 个配置槽：

要加载之前保存的不同配置，选择槽并点击“加载”。

配置完成后，选择槽并点击“保存”。

保存不同配置后，记得在设置框的输入标签中保存配置。

点击“下一个手柄”将循环浏览已分配的 PSX 控制器。

可以选择反转 PSX 模拟摇杆的 Y 轴，默认为“正常 Y”。

“菜单组合”配置允许您选择返回菜单的按钮组合。

设置：

常规：

本地保存设备：选择加载和保存本地游戏存档的位置。

存档设备：选择加载和保存存档的位置。

选择 CPU 核心：选择使用纯解释器（更好的兼容性）还是动态重编译（更好的速度）来运行游戏。

保存 settings.cfg：将所有这些设置保存到 SD 或 USB 中（下次启动时自动加载）。

视频：

显示 FPS：在屏幕左上角显示帧率。

屏幕模式：选择显示的宽高比；‘强制 16:9’模式将使游戏显示具有黑边。

输入：

配置输入：选择游戏中的控制器。

配置按钮：进入上述控制器配置屏幕。

保存按钮配置：将所有控制器配置槽保存到 SD 或 USB。

自动加载槽：为每种类型的控制器选择自动加载的槽。

音频：

禁用音频：选择是否静音。

保存：

自动保存本地存档：启用时，模拟器将在加载 ISO 时自动从选定的设备加载存档，并在返回菜单或关闭主机时保存。

复制存档：尚未实现。

删除存档：尚未实现。

兼容性：

如遇问题，请反馈至 https://github.com/emukidid/pcsxgc/issues

致谢：

一般编码：emu_kidid

图形与菜单编码：sepp256

音频与核心编码：tehpola

艺术设计：drmr

pcsx 团队：http://www.pcsx.net/

notaz for pcsx-ReARMed

pcercuei for lightrec：https://github.com/pcercuei/lightrec/

最初基于 Gil Pederson 提供的 pcsx 1.5-test3 Mac 版本：http://pcsx.gpost.dk/

P.E.Op.S. PSX GPU & SPU：http://sourceforge.net/projects/peops/

SSSPSX

使用最新的 devKitPro/libOGC 编译：http://sourceforge.net/projects/devkitpro

访问我们 IRC 频道：EFNet #gc-forever 或 www.gc-forever.com
 或 https://github.com/emukidid/pcsxgc/

更新日志：

请查看 GitHub 上的发布版本，本部分已不再维护。

Beta 4.0：

正确集成 libpsxcore，不再使用临时的修复来接收上游修复。

切换到上游 unai GPU 插件，带来巨大的性能提升。

启用三重缓冲。

切换到 cmake 构建系统。

Beta 3.0：

基于 pcercuei 的 lightrec 新 dynarec 技术，增加了速度和兼容性。

同步更新至 pcsx-ReARMed 上游版本，完全消除 Wii 上 XA/CDDA 的卡顿。

使用 dfsound 音频插件。

提高软图形插件的性能，感谢 Extrems 的建议。

支持 GameCube 的 SD2SP2。

修复了 UI 错误和内存泄漏问题。

Beta 2.1：

使用 devkitPPC r21 / libOGC SVN 编译。

新版 libDI / DVDx V2 编译。

保存改进：

修复保存没有写入存储卡的问题。

保存/加载音频状态。

控制器改进：

修复模拟输入的 Y 轴反转问题。

修复震动问题。

修复未使用输入端口的按键问题。

为经典控制器添加了“Home”按钮作为菜单组合。

添加禁用震动设置。

网络改进：

修复 SMB 允许匿名用户和密码。

增加线程化的网络初始化和错误信息。

Beta 2：

使用 devkitPPC r21 / libOGC SVN 编译。

新版 libDI / DVDx V2 编译。

使用直接 PowerPC 访问读取 DVD。

支持 Samba 加载。

执行 BIOS。

控制器改进：

支持 Wiimote 输入的震动。

仅支持 Wiimote 控制。

支持经典控制器 Pro 和第三方控制器。

支持重新配置按钮映射。

从文件保存/加载按钮映射。

新菜单系统：

支持经典控制器。

支持 Wiimote / Wiimote 和 Nunchuck。

支持设置保存。

自动保存/加载存档选项。

Beta 1：

音频工作（略有卡顿）。

DVD 加载功能。

使用 GX 缩放的软图形。

保存到 SD 卡。

文本 UI。

已知问题：

尚未实现 CDDA 音频流。

XA 音频偶尔失败。

《最终幻想 VII》会导致 Dynarec 崩溃。

FPS 限制偶尔失效。
